# Backend - copy to .env and fill in values
# Database (PostgreSQL). Default used by start-all.sh: postgres://idento:idento_password@localhost:5432/idento_db
DATABASE_URL=postgres://user:password@localhost:5432/idento_db

# Backend HTTP port (default: 8080)
PORT=8080

# JWT signing secret for auth tokens (required in production)
JWT_SECRET=your-secret-key-change-in-production
