<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production and all non-debuggable builds: cleartext HTTP is never permitted. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Applies ONLY to android:debuggable="true" builds (dev). Lets the emulator/simulator
         reach a local dev server over HTTP without weakening release security. -->
    <debug-overrides>
        <domain-config cleartextTrafficPermitted="true">
            <domain includeSubdomains="true">10.0.2.2</domain>
            <domain includeSubdomains="true">localhost</domain>
            <domain includeSubdomains="true">127.0.0.1</domain>
        </domain-config>
    </debug-overrides>
</network-security-config>
