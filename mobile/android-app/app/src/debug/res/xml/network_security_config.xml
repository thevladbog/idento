<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug variant only. Overrides src/main's config for debuggable builds: cleartext is still
  denied by default, but permitted for local dev servers (emulator loopback / localhost) so
  the app can talk to a dev backend over HTTP. Release builds never see this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
