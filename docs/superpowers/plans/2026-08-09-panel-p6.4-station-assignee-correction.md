# Panel P6.4 Station Assignee Correction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the 390×844 real-backend mobile journey pass by containing Overview overflow, exposing the readiness scroller to keyboard users, and requiring an explicit eligible staff/manager identity before station provisioning.

**Architecture:** Keep the two layout/a11y corrections local to the workspace components that own the broken DOM. `AddStationAction` owns its live `useEventStaff(eventId)` query and selector state, filters the API's full `User[]` response to `staff | manager`, and sends only the explicitly selected user id through the existing provisioning contract. The Playwright journey selects the seeded staff member by email and verifies the request subject without exposing the returned bearer.

**Tech Stack:** React 19, TypeScript 5.6, TanStack Query, Radix Select through `@idento/ui`, react-i18next, Vitest + Testing Library + MSW, Playwright + Axe.

**Spec:** [2026-08-09-panel-p6.4-hardening-parity-design.md](../specs/2026-08-09-panel-p6.4-hardening-parity-design.md) §5.2.1.

**Branch:** `feature/panel-p6.4-hardening-parity` in `/private/tmp/idento-panel-p6.4-hardening-parity`.

## Global Constraints

- Do not change backend authorization, OpenAPI, generated schema, database migrations, KMP, desktop, kiosk, printer, or agent code.
- `md` remains the only phone/desktop cutover; the reference viewport is exactly 390×844.
- Use only `@idento/ui` primitives and semantic color tokens in panel feature code.
- Add every user-facing string to both `panel/src/shared/i18n/en.json` and `panel/src/shared/i18n/ru.json` in the same commit.
- Do not auto-select an assignee, including when exactly one eligible user exists.
- Only event-assigned users whose live role is `staff` or `manager` are eligible; `admin` is never eligible.
- Loading, initial error, and empty eligible-list states remain distinguishable and fail closed without a provisioning request.
- A failed background refresh retains last-known-good assignees; a successful refresh that removes the selected assignee invalidates the selection.
- Phone-only selector triggers, options, and actions are at least 44×44 CSS px; body-level horizontal overflow is forbidden.
- QR bearer values remain ephemeral React/test variables. Never log, persist, serialize into assertion output, or place them in DOM text/accessibility attributes.
- Preserve unrelated worktree changes. Stage only the files named in each task.
- Physical iOS Safari, Android Chrome, KMP scanning, and cellular/SSE checks remain `NOT RUN` unless actually exercised on real devices.
- Human-authorized execution rule: any further failure reproduced by the real mobile journey inside `panel/` or `packages/ui/` may be split into a focused TDD prerequisite and reviewed without another approval pause. Backend, OpenAPI, generated schema, migration, KMP, desktop, kiosk, printer, and agent scope remains prohibited.
- Final-review exception authorized on 2026-08-10: backend/store/model/migration/OpenAPI changes are permitted only for Tasks 16-17 below, covering tenant-scoped self-service QR credentials and tenant-scoped `GetEventStaff` role semantics. All other non-web scope remains prohibited.

---

### Task 1: Contain Overview Width and Expose the Readiness Scroller

**Files:**
- Modify: `panel/src/features/workspace/EventWorkspaceLayout.tsx:100-106`
- Test: `panel/src/features/workspace/EventWorkspaceLayout.test.tsx`
- Modify: `panel/src/features/workspace/ReadinessStrip.tsx:17-24`
- Test: `panel/src/features/workspace/ReadinessStrip.test.tsx`

**Interfaces:**
- Consumes: the existing parent flex row, `<Outlet />`, phone-only horizontal readiness strip, and `workspaceRailLabel` translation.
- Produces: a shrinkable Outlet column and a named, keyboard-focusable horizontal-scroll region; no prop or API changes.

- [ ] **Step 1: Preserve the recorded RED regressions**

Keep the already-written class/semantics assertions. Their recorded baseline failures were:

```text
EventWorkspaceLayout: Outlet parent did not contain min-w-0
ReadinessStrip: readiness-strip-scroller had no tabindex or accessible region name
```

Do not revert production code merely to recreate these failures; inspect the focused test diff and retain these assertions:

```tsx
const overviewContent = screen.getByText("overview content");
expect(overviewContent.parentElement).toHaveClass("min-w-0");
expect(screen.getByTestId("readiness-strip-scroller")).toHaveAttribute("tabindex", "0");
expect(screen.getByRole("region", { name: "Readiness pipeline" })).toBeInTheDocument();
```

- [ ] **Step 2: Keep the minimal layout and accessibility implementation**

```tsx
<div className="min-w-0 flex-1 p-4 pb-24 md:p-6">
  <Outlet />
</div>
```

```tsx
<div
  data-testid="readiness-strip-scroller"
  role="region"
  aria-label={t("workspaceRailLabel")}
  tabIndex={0}
  className="flex min-w-0 max-w-full gap-1.5 overflow-x-auto"
>
```

- [ ] **Step 3: Run focused tests**

```bash
npm test -w panel -- EventWorkspaceLayout.test.tsx ReadinessStrip.test.tsx
```

Expected: both files PASS; the Outlet parent has `min-w-0`, and the scroller is discoverable as the localized `region`.

- [ ] **Step 4: Commit only the four workspace files**

```bash
git add panel/src/features/workspace/EventWorkspaceLayout.tsx panel/src/features/workspace/EventWorkspaceLayout.test.tsx panel/src/features/workspace/ReadinessStrip.tsx panel/src/features/workspace/ReadinessStrip.test.tsx
git diff --cached --check
git commit -m "fix(panel): contain mobile overview overflow"
```

---

### Task 2: Require an Explicit Eligible Station Assignee

**Files:**
- Modify: `panel/src/features/workspace/AddStationAction.tsx:1-100`
- Test: `panel/src/features/workspace/AddStationAction.test.tsx`
- Test: `panel/src/features/workspace/WorkspaceOverview.test.tsx:98-112`
- Modify: `panel/src/shared/i18n/en.json:107-112`
- Modify: `panel/src/shared/i18n/ru.json:107-112`
- Test: `panel/src/shared/i18n/keyParity.test.ts`

**Interfaces:**
- Consumes: `useEventStaff(eventId): UseQueryResult<components["schemas"]["User"][]>`, `STAFF_KEY(eventId)`, and the existing provisioning body `{ staff_user_id: string }`.
- Produces: `AddStationActionProps { eventId: string; eventName: string }`; internal `selectedStaffUserId: string`; a controlled `@idento/ui` Select with no default; unchanged `QrDisplay` session/regeneration behavior.

- [ ] **Step 1: Replace prop injection with a live staff endpoint in the test**

Import `act`, the generated schema type, and the exact query key:

```tsx
import { act, render, screen, waitFor } from "@testing-library/react";
import type { components } from "../../shared/api/schema";
import { STAFF_KEY } from "../staff/hooks";
```

```tsx
type StaffUser = components["schemas"]["User"];

function staffUser(id: string, email: string, role: StaffUser["role"]): StaffUser {
  return {
    id,
    tenant_id: "tenant-1",
    email,
    role,
    is_super_admin: false,
    has_qr_token: false,
    created_at: "2026-08-09T00:00:00Z",
    updated_at: "2026-08-09T00:00:00Z",
  };
}

let staffResponse: StaffUser[] = [];

http.get("http://api.test/api/events/:eventId/staff", () => HttpResponse.json(staffResponse)),
```

Make `renderAction()` return its `QueryClient` and render without `staffUserId`:

```tsx
function renderAction() {
  const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  render(
    <QueryClientProvider client={queryClient}>
      <AddStationAction eventId="evt-1" eventName="TechConf Moscow 2026" />
    </QueryClientProvider>,
  );
  return queryClient;
}
```

Reset each test to an admin plus two eligible users:

```tsx
staffResponse = [
  staffUser("admin-1", "owner@example.test", "admin"),
  staffUser("staff-1", "staff@example.test", "staff"),
  staffUser("manager-1", "manager@example.test", "manager"),
];
```

Add a successful handler to `WorkspaceOverview.test.tsx`'s module-scope server so mounting the nested action never makes an unhandled request:

```tsx
http.get("http://api.test/api/events/:eventId/staff", () => HttpResponse.json([{
  id: "staff-1",
  tenant_id: "tenant-1",
  email: "staff@example.test",
  role: "staff",
  is_super_admin: false,
  has_qr_token: false,
  created_at: "2026-08-09T00:00:00Z",
  updated_at: "2026-08-09T00:00:00Z",
}])),
```

- [ ] **Step 2: Write failing selector and authorization-boundary tests**

```tsx
it("requires an explicit staff or manager selection and never offers an admin", async () => {
  const user = userEvent.setup();
  renderAction();

  const addStation = await screen.findByRole("button", { name: /Добавить станцию/ });
  expect(addStation).toBeDisabled();
  await user.click(screen.getByRole("combobox", { name: "Назначенный сотрудник" }));
  expect(screen.queryByRole("option", { name: /owner@example\.test/ })).not.toBeInTheDocument();
  expect(screen.getByRole("option", { name: "staff@example.test · Сотрудник" })).toBeInTheDocument();
  expect(screen.getByRole("option", { name: "manager@example.test · Менеджер" })).toBeInTheDocument();
});
```

Add a one-eligible-user regression so Radix never visually or logically chooses the sole option on the operator's behalf:

```tsx
it("does not auto-select the only eligible user", async () => {
  staffResponse = [staffUser("staff-1", "staff@example.test", "staff")];
  renderAction();
  expect(await screen.findByRole("combobox", { name: "Назначенный сотрудник" })).toHaveTextContent("Выберите сотрудника");
  expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled();
});
```

Update the successful mint test to select before clicking and retain the exact body assertion:

```tsx
await user.click(await screen.findByRole("combobox", { name: "Назначенный сотрудник" }));
await user.click(screen.getByRole("option", { name: "staff@example.test · Сотрудник" }));
await user.click(screen.getByRole("button", { name: /Добавить станцию/ }));
expect(capturedBody).toEqual({ staff_user_id: "staff-1" });
```

Use one helper in every mint/error/regeneration test so all existing tests satisfy the explicit-selection contract:

```tsx
async function selectStaff(user: ReturnType<typeof userEvent.setup>) {
  await user.click(await screen.findByRole("combobox", { name: "Назначенный сотрудник" }));
  await user.click(screen.getByRole("option", { name: "staff@example.test · Сотрудник" }));
}
```

Add separate loading, initial-error, and empty tests with these state arrangements and assertions:

```tsx
it("shows loading and keeps provisioning disabled before staff resolves", () => {
  renderAction();
  expect(screen.getByText("Загрузка назначенных сотрудников…")).toBeInTheDocument();
  expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled();
});

it("shows an initial staff error distinct from the empty state", async () => {
  server.use(http.get("http://api.test/api/events/:eventId/staff", () =>
    HttpResponse.json({ error: "boom" }, { status: 500 }),
  ));
  renderAction();
  expect(await screen.findByRole("alert")).toHaveTextContent("Не удалось загрузить назначенных сотрудников.");
  expect(screen.queryByText("Для события нет доступных сотрудников или менеджеров.")).not.toBeInTheDocument();
  expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled();
});

it("shows an empty state when only an ineligible admin is assigned", async () => {
  staffResponse = [staffUser("admin-1", "owner@example.test", "admin")];
  renderAction();
  expect(await screen.findByText("Для события нет доступных сотрудников или менеджеров.")).toBeInTheDocument();
  expect(screen.queryByRole("alert")).not.toBeInTheDocument();
  expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled();
});
```

Add invalidation coverage using the exact query key:

```tsx
const queryClient = renderAction();
await user.click(await screen.findByRole("combobox", { name: "Назначенный сотрудник" }));
await user.click(screen.getByRole("option", { name: "staff@example.test · Сотрудник" }));
expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeEnabled();

act(() => {
  queryClient.setQueryData(STAFF_KEY("evt-1"), [
    staffUser("staff-1", "staff@example.test", "admin"),
    staffUser("manager-1", "manager@example.test", "manager"),
  ]);
});
await waitFor(() => expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled());
expect(screen.getByRole("combobox", { name: "Назначенный сотрудник" })).toHaveTextContent("Выберите сотрудника");
```

Extend invalidation through a successful mint to prove it is fail-closed after the QR session opens too:

```tsx
await selectStaff(user);
await user.click(screen.getByRole("button", { name: /Добавить станцию/ }));
await screen.findByTestId("qr-display-code");
act(() => {
  queryClient.setQueryData(STAFF_KEY("evt-1"), [
    staffUser("manager-1", "manager@example.test", "manager"),
  ]);
});
await waitFor(() => expect(screen.getByRole("button", { name: "Добавить станцию" })).toBeDisabled());
await user.click(screen.getByRole("button", { name: "Закрыть" }));
expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeDisabled();
```

Add a retained-data regression for background failure:

```tsx
it("retains a selected eligible user after a failed background refresh", async () => {
  const user = userEvent.setup();
  const queryClient = renderAction();
  await selectStaff(user);
  server.use(http.get("http://api.test/api/events/:eventId/staff", () =>
    HttpResponse.json({ error: "boom" }, { status: 500 }),
  ));
  await queryClient.invalidateQueries({ queryKey: STAFF_KEY("evt-1") });
  await waitFor(() => expect(queryClient.getQueryState(STAFF_KEY("evt-1"))?.status).toBe("error"));
  expect(screen.getByRole("combobox", { name: "Назначенный сотрудник" })).toHaveTextContent("staff@example.test");
  expect(screen.getByRole("button", { name: /Добавить станцию/ })).toBeEnabled();
  expect(screen.queryByRole("alert")).not.toBeInTheDocument();
});
```

- [ ] **Step 3: Run the new tests to verify RED**

```bash
npm test -w panel -- AddStationAction.test.tsx
```

Expected: FAIL because the component has no live query, combobox, translated states, filtering, or selection invalidation.

- [ ] **Step 4: Implement the live query and controlled eligibility state**

Remove `staffUserId` from the props and import:

```tsx
import {
  Label, QrDisplay, Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from "@idento/ui";
import { useEventStaff } from "../staff/hooks";
```

Inside `AddStationAction`:

```tsx
const staffQuery = useEventStaff(eventId);
const [selectedStaffUserId, setSelectedStaffUserId] = React.useState("");
const eligibleStaff = React.useMemo(
  () => (staffQuery.data ?? []).filter((user) => user.role === "staff" || user.role === "manager"),
  [staffQuery.data],
);
const selectedStaff = eligibleStaff.find((user) => user.id === selectedStaffUserId);

React.useEffect(() => {
  if (selectedStaffUserId && !selectedStaff) setSelectedStaffUserId("");
}, [selectedStaff, selectedStaffUserId]);

const staffInitialError = staffQuery.isError && !staffQuery.data;
```

Change the mutation guard/body to:

```tsx
if (!selectedStaff || mintInFlightRef.current) return;
// ...
body: { staff_user_id: selectedStaff.id },
```

Disable QR regeneration too if a successful staff refresh invalidates the selected identity:

```tsx
isRegenerating={mint.isPending || !selectedStaff}
```

- [ ] **Step 5: Render distinct fail-closed states and the phone-safe Select**

Render before the existing Add station button:

```tsx
<div className="mb-3 space-y-1.5">
  <Label htmlFor="station-assignee">{t("addStationAssigneeLabel")}</Label>
  {staffQuery.isLoading ? (
    <p role="status" className="text-caption text-muted-foreground">{t("addStationAssigneeLoading")}</p>
  ) : staffInitialError ? (
    <p role="alert" className="text-caption text-destructive">{t("addStationAssigneeError")}</p>
  ) : eligibleStaff.length === 0 ? (
    <p className="text-caption text-muted-foreground">{t("addStationAssigneeEmpty")}</p>
  ) : (
    <Select value={selectedStaff?.id ?? ""} onValueChange={setSelectedStaffUserId}>
      <SelectTrigger id="station-assignee" className="min-h-11">
        <SelectValue placeholder={t("addStationAssigneePlaceholder")} />
      </SelectTrigger>
      <SelectContent>
        {eligibleStaff.map((user) => (
          <SelectItem key={user.id} value={user.id} className="min-h-11">
            {user.email} · {t(user.role === "manager" ? "staffRoleManager" : "staffRoleStaff")}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  )}
</div>
```

The action uses:

```tsx
disabled={!selectedStaff || mint.isPending}
```

- [ ] **Step 6: Add exact EN/RU copy**

```json
"addStationAssigneeLabel": "Assigned staff",
"addStationAssigneePlaceholder": "Select staff",
"addStationAssigneeLoading": "Loading assigned staff…",
"addStationAssigneeError": "Couldn't load assigned staff.",
"addStationAssigneeEmpty": "No assigned staff or managers are available for this event."
```

```json
"addStationAssigneeLabel": "Назначенный сотрудник",
"addStationAssigneePlaceholder": "Выберите сотрудника",
"addStationAssigneeLoading": "Загрузка назначенных сотрудников…",
"addStationAssigneeError": "Не удалось загрузить назначенных сотрудников.",
"addStationAssigneeEmpty": "Для события нет доступных сотрудников или менеджеров."
```

Do not change `keyParity.test.ts`; run its existing exact-key parity assertions.

- [ ] **Step 7: Run focused GREEN and static checks**

```bash
npm test -w panel -- AddStationAction.test.tsx keyParity.test.ts
npm run typecheck -w panel
npm run lint -w panel
```

Expected: all commands exit 0; existing QR regeneration/session tests remain green after selecting an eligible fixture user.

- [ ] **Step 8: Commit the selector atomically**

```bash
git add panel/src/features/workspace/AddStationAction.tsx panel/src/features/workspace/AddStationAction.test.tsx panel/src/features/workspace/WorkspaceOverview.test.tsx panel/src/shared/i18n/en.json panel/src/shared/i18n/ru.json
git diff --cached --check
git commit -m "fix(panel): require station assignee selection"
```

---

### Task 3: Bind the Real-Backend Mobile Journey to the Seeded Staff User

**Files:**
- Modify: `panel/e2e/mobile-companion.spec.ts:300-336`
- Read: `panel/e2e/fixtures/seedMobileCompanion.ts:14-31,75-110`

**Interfaces:**
- Consumes: `MobileSeed.staff: { id: string; email: string }`, the English combobox name `Assigned staff`, and the existing credential-safe response helpers.
- Produces: a real-backend provisioning request whose `staff_user_id` equals `seed.staff.id`; no new artifact captures and no credential output.

- [ ] **Step 1: Add the explicit selection before the request waiter**

Insert after the Overview touch/overflow assertions:

```tsx
const assignee = page.getByRole("combobox", { name: "Assigned staff" });
await expect(assignee).toBeVisible();
await expectTouchTargetsAtLeast44(assignee);
await assignee.click();
const seededStaffOption = page.getByRole("option", { name: `${seed.staff.email} · Staff` });
await expect(seededStaffOption).toBeVisible();
await expectTouchTargetsAtLeast44(seededStaffOption);
await seededStaffOption.click();
await expect(page.getByRole("button", { name: /Add station/ })).toBeEnabled();
```

- [ ] **Step 2: Verify the request subject without serializing its response**

After receiving `provisioningResponse`, inspect only the request body:

```tsx
const provisioningRequestBody = provisioningResponse.request().postDataJSON() as { staff_user_id?: string };
expect(provisioningRequestBody.staff_user_id).toBe(seed.staff.id);
```

Keep `credentialFrom`, `expectBearerAbsent`, the browser-local QR fingerprint, and mobile project `trace/screenshot/video: off` unchanged. Do not include the response body or QR SVG in assertion messages.

- [ ] **Step 3: Run compile and discovery checks**

```bash
npx tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --esModuleInterop --skipLibCheck --types node,@playwright/test panel/e2e/mobile-companion.spec.ts panel/e2e/mobile-assertions.ts panel/e2e/fixtures/e2eAuth.ts panel/e2e/fixtures/seedCheckinEvent.ts panel/e2e/fixtures/seedMobileCompanion.ts
npm run e2e -w panel -- --list
```

Expected: TypeScript exits 0; discovery remains exactly 7 desktop tests under `chromium` and 3 mobile tests under `mobile-chromium`.

- [ ] **Step 4: Run the functional mobile journey against the isolated real backend**

With the isolated Postgres/backend on their task-specific ports and required admin environment present, run outside the restricted sandbox:

```bash
npm run e2e -w panel -- --project=mobile-chromium panel/e2e/mobile-companion.spec.ts --grep "light-theme functional journey"
```

Expected: PASS through Overview overflow/Axe, explicit assignee selection, provisioning, regeneration, Monitor recovery, attendee mutations, staff QR, and staff self-service. If a new failure appears, apply `superpowers:systematic-debugging`, add a focused RED regression, and fix the production cause before rerunning.

- [ ] **Step 5: Run all three mobile tests against the same real backend**

```bash
npm run e2e -w panel -- --project=mobile-chromium panel/e2e/mobile-companion.spec.ts
```

Expected: 3/3 PASS. Stop only the task-created backend/database resources; preserve pre-existing containers and services.

- [ ] **Step 6: Commit the E2E binding**

```bash
git add panel/e2e/mobile-companion.spec.ts
git diff --cached --check
git commit -m "test(panel): bind station provisioning to seeded staff"
```

---

### Task 4: Restore WCAG AA Contrast for the Shared QR Hint

**Files:**
- Modify: `packages/ui/src/components/qr-display.tsx:169`
- Test: `packages/ui/src/components/qr-display.test.tsx:27-66`

**Interfaces:**
- Consumes: `QrDisplayProps.hint?: string` and the intentionally fixed white QR surface.
- Produces: the same optional hint with at least WCAG AA 4.5:1 contrast; no prop, DOM, persistence, or credential-flow changes.

- [ ] **Step 1: Write the failing contrast-class regression**

Extend the existing title/subtitle/QR/hint test:

```tsx
const hint = screen.getByText(/Have Anna scan this/);
expect(hint).toHaveClass("text-black/60");
expect(hint).not.toHaveClass("text-black/50");
```

- [ ] **Step 2: Run the focused test to verify RED**

```bash
npm test -w @idento/ui -- qr-display.test.tsx
```

Expected: FAIL because the hint still has `text-black/50`, the class observed by the real-browser Axe failure at 3.94:1.

- [ ] **Step 3: Apply the minimal shared-component fix**

Change only the hint opacity:

```tsx
{hint ? <p className="mt-auto max-w-[280px] pt-8 text-caption text-black/60">{hint}</p> : null}
```

Black at 60% on the fixed white QR surface is darker than the failing 50% rendering and clears the required 4.5:1 threshold without changing QR layout, content, or accessible naming.

- [ ] **Step 4: Run focused and package gates**

```bash
npm test -w @idento/ui -- qr-display.test.tsx
npm test -w @idento/ui
npm run typecheck -w @idento/ui
npm run lint -w @idento/ui
```

Expected: all commands exit 0; the focused test proves the contrast class and the full UI package remains green.

- [ ] **Step 5: Commit only the two shared UI files**

```bash
git add packages/ui/src/components/qr-display.tsx packages/ui/src/components/qr-display.test.tsx
git diff --cached --check
git commit -m "fix(ui): increase QR hint contrast"
```

After Task 4 passes review, resume Task 3 at its real-backend focused run and then its three-test mobile run.

---

### Task 5: Require Accessible Names for Every Shared Progress Bar

**Files:**
- Modify: `packages/ui/src/components/progress.tsx:3-18`
- Test: `packages/ui/src/components/progress.test.tsx`
- Modify: `panel/src/features/attendees/import/ImportWizard.tsx:1095-1104`
- Modify: `panel/src/features/home/LiveStrip.tsx:122-128`
- Test: `panel/src/features/home/LiveStrip.test.tsx:110-128`
- Modify: `panel/src/features/monitor/TotalsCard.tsx:72-78`
- Test: `panel/src/features/monitor/TotalsCard.test.tsx`
- Modify: `panel/src/features/monitor/ZonesCard.tsx:36-52`
- Test: `panel/src/features/monitor/MonitorPage.test.tsx:250-280`
- Modify: `panel/src/shared/i18n/en.json`
- Modify: `panel/src/shared/i18n/ru.json`
- Test unchanged: `panel/src/shared/i18n/keyParity.test.ts`

**Interfaces:**
- Consumes: every current `<Progress>` call in the repository and app-owned localized copy.
- Produces: required `ProgressProps.ariaLabel: string`, forwarded as `aria-label` on `role="progressbar"`; no `@idento/ui` i18n dependency.

- [ ] **Step 1: Write the failing shared-contract test**

Update every `Progress` render in `progress.test.tsx` to pass `ariaLabel="Upload progress"`, then strengthen the value-attribute test:

```tsx
render(<Progress value={25} max={50} ariaLabel="Upload progress" />);
const bar = screen.getByRole("progressbar", { name: "Upload progress" });
expect(bar).toHaveAttribute("aria-label", "Upload progress");
```

- [ ] **Step 2: Write failing localized consumer assertions**

Strengthen the existing running-event test:

```tsx
expect(screen.getByRole("progressbar", { name: "Event check-in progress" })).toBeInTheDocument();
```

Add to `TotalsCard.test.tsx`:

```tsx
it("names the check-in progress bar", () => {
  render(<TotalsCard totals={TOTALS_FIXTURE} />);
  expect(screen.getByRole("progressbar", { name: "Event check-in progress" })).toBeInTheDocument();
});
```

In `MonitorPage.test.tsx`'s successful snapshot test, assert the totals and zone bars:

```tsx
expect(screen.getByRole("progressbar", { name: "Event check-in progress" })).toBeInTheDocument();
expect(screen.getByRole("progressbar", { name: "Main hall check-in progress" })).toBeInTheDocument();
expect(screen.getByRole("progressbar", { name: "VIP check-in progress" })).toBeInTheDocument();
expect(screen.getByRole("progressbar", { name: "Backstage check-in progress" })).toBeInTheDocument();
```

- [ ] **Step 3: Run focused tests to verify RED**

```bash
npm test -w @idento/ui -- progress.test.tsx
npm test -w panel -- LiveStrip.test.tsx TotalsCard.test.tsx MonitorPage.test.tsx
```

Expected: FAIL because `Progress` has no `ariaLabel` contract and the rendered bars have no accessible names.

- [ ] **Step 4: Make the accessible name required in the shared primitive**

```tsx
export interface ProgressProps {
  value: number;
  max: number;
  ariaLabel: string;
  className?: string;
}

export function Progress({ value, max, ariaLabel, className }: ProgressProps) {
  // existing percentage calculation
  return (
    <div
      role="progressbar"
      aria-label={ariaLabel}
      aria-valuenow={value}
      aria-valuemin={0}
      aria-valuemax={max}
      className={cn("h-1.5 w-full overflow-hidden rounded-full bg-muted", className)}
    >
      {/* existing fill */}
    </div>
  );
}
```

- [ ] **Step 5: Supply localized names at every current consumer**

```tsx
// ImportWizard
<Progress value={done} max={total} ariaLabel={t("importProgressLabel")} />

// LiveStrip and TotalsCard
<Progress value={checkedIn} max={total} ariaLabel={t("checkInProgressLabel")} className="w-full md:w-56" />
<Progress value={totals.checked_in} max={totals.total} ariaLabel={t("checkInProgressLabel")} />

// ZonesCard
<Progress
  value={zone.checked_in}
  max={checkedInTotal}
  ariaLabel={t("monitorZoneProgressLabel", { zone: zone.name })}
  className="h-1.5"
/>
<Progress
  value={unattributed}
  max={checkedInTotal}
  ariaLabel={t("monitorZoneProgressLabel", { zone: t("monitorUnattributed") })}
  className="h-1.5"
/>
```

- [ ] **Step 6: Add exact EN/RU copy**

```json
"checkInProgressLabel": "Event check-in progress",
"importProgressLabel": "Attendee import progress",
"monitorZoneProgressLabel": "{{zone}} check-in progress"
```

```json
"checkInProgressLabel": "Прогресс регистрации на событие",
"importProgressLabel": "Прогресс импорта участников",
"monitorZoneProgressLabel": "Прогресс регистрации: {{zone}}"
```

- [ ] **Step 7: Run focused and full gates**

```bash
npm test -w @idento/ui -- progress.test.tsx
npm test -w panel -- LiveStrip.test.tsx TotalsCard.test.tsx MonitorPage.test.tsx keyParity.test.ts
npm test -w @idento/ui
npm run typecheck -w @idento/ui
npm run lint -w @idento/ui
npm run typecheck -w panel
npm run lint -w panel
```

Expected: every command exits 0. Required `ariaLabel` plus panel typecheck proves no current `Progress` consumer remains unnamed.

- [ ] **Step 8: Commit the primitive contract and all consumers atomically**

```bash
git add packages/ui/src/components/progress.tsx packages/ui/src/components/progress.test.tsx panel/src/features/attendees/import/ImportWizard.tsx panel/src/features/home/LiveStrip.tsx panel/src/features/home/LiveStrip.test.tsx panel/src/features/monitor/TotalsCard.tsx panel/src/features/monitor/TotalsCard.test.tsx panel/src/features/monitor/ZonesCard.tsx panel/src/features/monitor/MonitorPage.test.tsx panel/src/shared/i18n/en.json panel/src/shared/i18n/ru.json
git diff --cached --check
git commit -m "fix(ui): require progress bar accessible names"
```

After Task 5 passes review, resume Task 3 from its focused real-backend run and continue to all three mobile tests.

---

### Task 6: Preserve Monitor Text Contrast While Dimming Retained Data

**Files:**
- Modify: `panel/src/features/monitor/MonitorPage.tsx:198-208`
- Test: `panel/src/features/monitor/MonitorPage.test.tsx:730-765`

**Interfaces:**
- Consumes: retained snapshot body state `snapshot && !live`, existing `opacity-60`, and semantic descendant classes such as `text-muted-foreground`.
- Produces: the same degraded-state dimming and stale/reconnecting semantics, with muted descendants promoted to `text-foreground` before parent opacity compositing so rendered small text remains WCAG AA compliant.

- [ ] **Step 1: Extend the degraded-state regression test**

In the existing reconnecting opacity test, strengthen the degraded and restored assertions:

```tsx
streamConnections[0].close();
await waitFor(() => expect(screen.getByTestId("monitor-reconnecting-badge")).toBeInTheDocument());
expect(screen.getByTestId("monitor-body")).toHaveClass(
  "opacity-60",
  "[&_.text-muted-foreground]:text-foreground",
);

streamConnections[1].push("event: hello\ndata: {}\n\n");
await waitFor(() => expect(screen.queryByTestId("monitor-reconnecting-badge")).not.toBeInTheDocument());
expect(screen.getByTestId("monitor-body")).not.toHaveClass(
  "opacity-60",
  "[&_.text-muted-foreground]:text-foreground",
);
```

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- MonitorPage.test.tsx
```

Expected: FAIL because the retained body has `opacity-60` but no descendant contrast compensation.

- [ ] **Step 3: Add the degraded-only semantic override**

Keep the existing full-body dimming and add one scoped descendant override only in degraded retained-data state:

```tsx
snapshot && !live && "opacity-60 [&_.text-muted-foreground]:text-foreground",
```

Update the adjacent comment to record the measured failure: `text-muted-foreground` at parent 60% opacity rendered 2.53:1 on the light background, so degraded descendants must start from `text-foreground`. Do not change individual card copy/classes, live-state hierarchy, or the warning/reconnecting status vocabulary.

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- MonitorPage.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: all commands exit 0; focused tests retain the existing 60% degraded-state behavior and prove the contrast override is removed when live.

- [ ] **Step 5: Commit only the Monitor root and its test**

```bash
git add panel/src/features/monitor/MonitorPage.tsx panel/src/features/monitor/MonitorPage.test.tsx
git diff --cached --check
git commit -m "fix(panel): preserve degraded monitor contrast"
```

After Task 6 passes review, resume Task 3 at the focused real-backend degraded-state Axe checkpoint and continue to all three mobile tests.

---

### Task 7: Restore Attendee Success-State Contrast

**Files:**
- Modify: `panel/src/app/App.tsx:1-22`
- Test: `panel/src/app/App.test.tsx`
- Modify: `panel/src/features/attendees/AttendeeCard.tsx:188-194`
- Test: `panel/src/features/attendees/AttendeeCard.test.tsx:75-90`

**Interfaces:**
- Consumes: Sonner's existing `richColors` toaster, panel semantic success tokens, and the manual check-in button subcaption.
- Produces: WCAG AA attendee success-state text contrast in light and dark themes without changing toast behavior, action copy, DOM credential handling, or API contracts.

- [ ] **Step 1: Write failing semantic-token and subcaption regressions**

In `App.test.tsx`, render the app and assert the Sonner host overrides its success palette with the panel's semantic token variables:

```tsx
const toaster = document.querySelector<HTMLElement>("[data-sonner-toaster]");
expect(toaster).not.toBeNull();
expect(toaster?.style.getPropertyValue("--success-bg")).toBe("var(--success)");
expect(toaster?.style.getPropertyValue("--success-border")).toBe("var(--success)");
expect(toaster?.style.getPropertyValue("--success-text")).toBe("var(--success-foreground)");
```

In the existing attendee action test, strengthen the subcaption assertion:

```tsx
const subcaption = screen.getByText("бейдж не будет напечатан");
expect(subcaption).toHaveClass("text-primary-foreground");
expect(subcaption).not.toHaveClass("text-primary-foreground/85");
```

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- App.test.tsx AttendeeCard.test.tsx
```

Expected: FAIL because Sonner still uses its built-in light rich-success colors measured at 4.20:1 and the button subcaption still uses 85% foreground measured at 4.19:1.

- [ ] **Step 3: Apply the minimal semantic-token fixes**

Keep `richColors`, but provide theme-dynamic semantic variables on the toaster host:

```tsx
import type { CSSProperties } from "react";

const TOASTER_STYLE = {
  "--success-bg": "var(--success)",
  "--success-border": "var(--success)",
  "--success-text": "var(--success-foreground)",
} as CSSProperties;

<Toaster position="top-right" richColors style={TOASTER_STYLE} />
```

Change only the manual check-in subcaption class from `text-primary-foreground/85` to `text-primary-foreground`. Do not change toast text, duration, positioning, button layout, or any credential-bearing behavior.

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- App.test.tsx AttendeeCard.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: all commands exit 0 and the two measured contrast causes have exact regressions.

- [ ] **Step 5: Commit only the four attendee/toaster files**

```bash
git add panel/src/app/App.tsx panel/src/app/App.test.tsx panel/src/features/attendees/AttendeeCard.tsx panel/src/features/attendees/AttendeeCard.test.tsx
git diff --cached --check
git commit -m "fix(panel): restore attendee success contrast"
```

After Task 7 passes review, resume Task 3 at the focused real-backend attendee Axe checkpoint and continue to all three mobile tests.

---

### Task 8: Restore Focus After Closing the Staff QR Dialog

**Files:**
- Modify: `panel/src/features/staff/StaffCard.tsx:135-145,315-330,467-490`
- Test: `panel/src/features/staff/StaffCard.test.tsx:195-265`

**Interfaces:**
- Consumes: the controlled Radix full-screen QR `Dialog`, the plain `Show full screen` button, and every existing close path (`X`, Back to card, Escape, outside dismissal).
- Produces: deterministic focus restoration to the exact button that opened the dialog; no changes to QR token state, dialog content, authorization, minting, printing, or credential handling.

- [ ] **Step 1: Extend the existing dialog regression with failing focus assertions**

Keep a reference to the opener before activation and require it to regain focus after close. Cover at least the visible QR close path exercised by the real journey and Escape:

```tsx
const showFullScreen = await screen.findByRole("button", { name: "Show full screen" });
await user.click(showFullScreen);
await screen.findByRole("dialog", { name: "Staff login — alice@example.com" });
await user.click(screen.getByRole("button", { name: "Close" }));
await waitFor(() => expect(screen.queryByRole("dialog")).not.toBeInTheDocument());
expect(showFullScreen).toHaveFocus();
```

Retain the existing Escape test and add the same final focus assertion there.

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- StaffCard.test.tsx
```

Expected: FAIL because the controlled dialog has no `DialogTrigger` association and its opener is a plain button, so Radix cannot restore focus after unmount.

- [ ] **Step 3: Add explicit opener focus restoration**

Create a button ref and attach it to the existing `Show full screen` button:

```tsx
const fullScreenTriggerRef = React.useRef<HTMLButtonElement>(null);

<button ref={fullScreenTriggerRef} ...>
```

Handle Radix's close autofocus event on the existing `DialogContent`:

```tsx
onCloseAutoFocus={(event) => {
  event.preventDefault();
  fullScreenTriggerRef.current?.focus();
}}
```

Do not move the dialog root around the card, introduce a hidden trigger, mint a new token, or place any token value in DOM attributes/assertion messages. If the opener is absent, the optional ref leaves focus behavior fail-safe without throwing.

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- StaffCard.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: all commands exit 0; both close paths restore focus to the exact opener and existing trap/Escape/QR behavior remains green.

- [ ] **Step 5: Commit only the StaffCard and its test**

```bash
git add panel/src/features/staff/StaffCard.tsx panel/src/features/staff/StaffCard.test.tsx
git diff --cached --check
git commit -m "fix(panel): restore staff QR dialog focus"
```

After Task 8 passes review, resume Task 3 at the final staff QR close checkpoint and continue to all three mobile tests.

---

### Task 9: Tolerate Browser Subpixel Rounding in Touch-Target Checks

**Files:**
- Create: `panel/e2e/mobile-geometry.ts`
- Modify: `panel/e2e/mobile-assertions.ts:12-25`
- Create: `panel/src/test/mobileGeometry.test.ts`

**Interfaces:**
- Consumes: Playwright `boundingBox()` floating-point width/height and the exact 44 CSS px WCAG/mobile product minimum.
- Produces: a pure geometry predicate with a maximum 0.001 CSS px rounding tolerance; genuine targets below that boundary remain failures.

- [ ] **Step 1: Write the pure RED boundary regression**

Create a focused unit test for the exact browser observation plus a nearby negative control:

```ts
expect(meetsMinimumTouchTarget(44)).toBe(true);
expect(meetsMinimumTouchTarget(43.99993896484375)).toBe(true);
expect(meetsMinimumTouchTarget(43.998)).toBe(false);
```

The negative control is load-bearing: do not use `Math.round`, `Math.ceil`, integer truncation, or a tolerance large enough to accept `43.998px`.

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- mobileGeometry.test.ts
```

Expected: FAIL because the pure geometry helper does not exist yet. Preserve the twice-reproduced real-browser evidence `43.99993896484375 >= 44` in the task report without weakening the production CSS target.

- [ ] **Step 3: Add the minimal geometry helper and wire it into Playwright assertions**

```ts
export const MINIMUM_TOUCH_TARGET_CSS_PX = 44;
const SUBPIXEL_ROUNDING_TOLERANCE_CSS_PX = 0.001;

export function meetsMinimumTouchTarget(size: number): boolean {
  return size >= MINIMUM_TOUCH_TARGET_CSS_PX - SUBPIXEL_ROUNDING_TOLERANCE_CSS_PX;
}
```

Use this predicate for both `box.width` and `box.height` in `expectTouchTargetsAtLeast44`, with an assertion message that identifies the element index, axis, measured size, and exact 44px requirement but contains no DOM markup or credential values.

- [ ] **Step 4: Run focused, compile, discovery, and static GREEN**

```bash
npm test -w panel -- mobileGeometry.test.ts
npx tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --esModuleInterop --skipLibCheck --types node,@playwright/test panel/e2e/mobile-companion.spec.ts panel/e2e/mobile-assertions.ts panel/e2e/mobile-geometry.ts panel/e2e/fixtures/e2eAuth.ts panel/e2e/fixtures/seedCheckinEvent.ts panel/e2e/fixtures/seedMobileCompanion.ts
npm run e2e -w panel -- --list
npm run typecheck -w panel
npm run lint -w panel
```

Expected: focused unit tests exit 0; E2E compile exits 0; discovery remains exactly 7 desktop + 3 mobile; static gates remain green.

- [ ] **Step 5: Commit only the geometry helper, assertion, and unit regression**

```bash
git add panel/e2e/mobile-geometry.ts panel/e2e/mobile-assertions.ts panel/src/test/mobileGeometry.test.ts
git diff --cached --check
git commit -m "test(panel): tolerate subpixel touch rounding"
```

After Task 9 passes review, resume Task 3 with a fresh real backend and require the complete three-test mobile serial run to pass.

---

### Task 10: Restore WCAG AA Contrast for Live Monitor Metadata

**Files:**
- Modify: `panel/src/features/monitor/TotalsCard.tsx:64-90`
- Test: `panel/src/features/monitor/TotalsCard.test.tsx`
- Modify: `panel/src/features/monitor/StationsCard.tsx:89-104`
- Modify: `panel/src/features/monitor/RecentFeedCard.tsx:100-116`
- Test: `panel/src/features/monitor/MonitorPage.test.tsx:390-470,500-545`

**Interfaces:**
- Consumes: live/recovered Monitor snapshot cards and their small percent, rate, station-state, and timestamp metadata.
- Produces: the same copy, layout, hierarchy, and live/degraded state semantics with small live-state metadata using contrast-safe semantic foreground; global color tokens and degraded root behavior remain unchanged.

- [ ] **Step 1: Write failing class regressions for all six measured nodes**

In `TotalsCard.test.tsx`, assert the percentage and shared three-part rate row use full semantic foreground and no longer use the failing muted class:

```tsx
expect(screen.getByText("53%")).toHaveClass("text-foreground");
const row = screen.getByTestId("monitor-rate-row");
expect(row).toHaveClass("text-foreground");
expect(row).not.toHaveClass("text-muted-foreground");
```

In the existing MonitorPage station and recent-feed tests, assert:

```tsx
const online = screen.getByTestId("monitor-station-online-st-1");
expect(online).toHaveClass("text-foreground");
expect(online).not.toHaveClass("text-muted-foreground");

const timestamp = screen.getByText("09:05:03");
expect(timestamp).toHaveClass("text-foreground");
expect(timestamp).not.toHaveClass("text-muted-foreground");
```

These cover the six real Axe nodes: one percent, three inherited rate spans, one Online label, and one timestamp.

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- TotalsCard.test.tsx MonitorPage.test.tsx
```

Expected: FAIL because all measured nodes still use `text-muted-foreground`, rendered by Chromium at `#97979c` on `#fdfdfd` = 2.85:1.

- [ ] **Step 3: Promote only the failing small live metadata**

Change these exact classes from `text-muted-foreground` to `text-foreground`:

- Totals percentage span.
- Totals three-part rate-row container.
- Fresh station `Online` span.
- Recent-feed timestamp span.

Do not change global tokens, station dots, stale/warning copy, recent action icons, optional zone copy, degraded `opacity-60` handling, fonts, sizing, spacing, or strings. Typography and placement continue to express secondary hierarchy without relying on low contrast.

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- TotalsCard.test.tsx MonitorPage.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: all commands exit 0; exact class regressions cover every node reported by the recovered/live Axe checkpoint.

- [ ] **Step 5: Commit only the five Monitor files**

```bash
git add panel/src/features/monitor/TotalsCard.tsx panel/src/features/monitor/TotalsCard.test.tsx panel/src/features/monitor/StationsCard.tsx panel/src/features/monitor/RecentFeedCard.tsx panel/src/features/monitor/MonitorPage.test.tsx
git diff --cached --check
git commit -m "fix(panel): restore live monitor contrast"
```

After Task 10 passes review, resume Task 3 with a fresh real backend and require the complete three-test mobile serial run to pass.

---

### Task 11: Make Attendee Drawer Pending-Cancel Test Deterministic

**Files:**
- Test: `panel/src/features/attendees/AttendeeDrawer.test.tsx`

**Interfaces:**
- Consumes: the existing `createGate()` test helper, DELETE request entry, cancel dismissal, and the production session guard that ignores a settled response after dismissal.
- Produces: the same product assertions without relying on a 50ms wall-clock race under full Vitest parallel load; no production change.

- [ ] **Step 1: Preserve the recorded full-suite RED evidence**

The same Task 9 test failed in two fresh default full-panel runs while passing focused 41/41 and in a combined 76/76 run. Its fixed MSW `await delay(50)` can settle during `await user.click(Delete)` under load, allowing production to correctly call `onClose` before the test reaches Cancel.

Do not add a longer timeout or delay. Replace only this test's fixed-delay synchronization with the file's existing manually released `createGate()` helper.

- [ ] **Step 2: Gate the DELETE response on explicit test phases**

Arrange the DELETE handler to record request entry, then await the gate before returning 204. Exercise the sequence explicitly:

```tsx
const gate = createGate();
server.use(http.delete("http://api.test/api/events/:eventId/attendees/:attendeeId", async () => {
  gate.markEntered();
  await gate.wait;
  return new HttpResponse(null, { status: 204 });
}));

await user.click(screen.getByRole("button", { name: "Удалить" }));
await gate.entered;
await user.click(screen.getByRole("button", { name: "Отмена" }));
expect(screen.queryByRole("dialog")).not.toBeInTheDocument();
expect(onClose).not.toHaveBeenCalled();

gate.release();
await waitFor(() => expect(onClose).not.toHaveBeenCalled());
```

Keep every existing assertion about pending state, dismissed dialog, invalidation/session guard, and no stale close callback. Use `try/finally` or equivalent cleanup so the gate is always released if an assertion fails.

- [ ] **Step 3: Run focused and repeated GREEN**

```bash
npm test -w panel -- AttendeeDrawer.test.tsx
npm test -w panel -- AttendeeDrawer.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: both focused runs pass 41/41 and static gates exit 0. The default full-suite consecutive pair remains Task 12's fresh acceptance gate.

- [ ] **Step 4: Commit only the deterministic test**

```bash
git add panel/src/features/attendees/AttendeeDrawer.test.tsx
git diff --cached --check
git commit -m "test(panel): gate attendee dismissal race"
```

---

### Task 12: Make Add Staff Pending-Dismiss Test Deterministic

**Files:**
- Test: `panel/src/features/staff/AddStaffDialog.test.tsx`

**Interfaces:**
- Consumes: the existing session-ref pending-dismiss regression, assign POST entry, blocked Cancel/Escape behavior while pending, and successful close after response settlement.
- Produces: the same product assertions with response timing controlled by an explicit manually released gate instead of `assignDelayMs = 60`; no production change.

- [ ] **Step 1: Preserve the recorded full-suite RED evidence**

The default full-panel run failed because the fixed 60ms assign delay settled during awaited `userEvent` interactions under parallel load. The focused test passes 1/1. Production correctly closes on success; the test must prove the pending phase before attempting Cancel/Escape.

Do not lengthen the delay or timeout. Replace only this test's wall-clock timing with explicit request-entry and response-release signals.

- [ ] **Step 2: Gate the assign response and assert both phases**

Use a local request gate with `entered`, `markEntered`, `wait`, and `release`, and override only the assign POST inside this test. The handler must increment the existing request counter and return the same valid 201 assignment after release.

```tsx
await user.click(await screen.findByText("bob@example.com"));
await user.click(screen.getByRole("button", { name: "Add" }));
await gate.entered;

await user.click(screen.getAllByRole("button", { name: "Cancel" })[0]);
await user.keyboard("{Escape}");
expect(onOpenChange).not.toHaveBeenCalled();

gate.release();
await waitFor(() => expect(onOpenChange).toHaveBeenCalledWith(false));
```

Always release the gate in `finally` so a failed assertion cannot hang the worker. Preserve the request-count assertion and the distinction between blocked dismissal while pending and legitimate close after success.

- [ ] **Step 3: Run focused and repeated GREEN**

```bash
npm test -w panel -- AddStaffDialog.test.tsx
npm test -w panel -- AddStaffDialog.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

Expected: both focused runs pass all AddStaffDialog tests and static gates exit 0. The consecutive default full-suite pair remains Task 13's acceptance gate.

- [ ] **Step 4: Commit only the deterministic test**

```bash
git add panel/src/features/staff/AddStaffDialog.test.tsx
git diff --cached --check
git commit -m "test(panel): gate staff assignment dismissal"
```

---

### Task 13: Re-run Required Gates, Push, and Reconcile the PR

**Files:**
- Modify append-only if verification evidence changes: `.superpowers/sdd/progress.md`
- Create: `.superpowers/sdd/2026-08-09-panel-p6.4-hardening-parity/station-assignee-correction-report.md`

**Interfaces:**
- Consumes: Tasks 1-12 commits and PR #113 check/review state.
- Produces: fresh local evidence, a pushed branch, resolved actionable review threads, and an explicit status for the external GitGuardian incident.

- [ ] **Step 1: Run two consecutive full panel suites**

```bash
npm test -w panel
npm test -w panel
```

Expected: both consecutive runs exit 0. A failure resets the consecutive count; diagnose it before restarting the pair.

- [ ] **Step 2: Run shared UI and panel static/build gates**

```bash
npm test -w @idento/ui
npm run typecheck -w @idento/ui
npm run lint -w @idento/ui
npm run typecheck -w panel
npm run lint -w panel
npm run build -w panel
npm run check:bundle -w panel
```

Expected: all commands exit 0 and bundle output stays within 1,550,000 raw bytes and 430,000 gzip bytes.

- [ ] **Step 3: Re-run Playwright discovery and available real-backend suites**

```bash
npm run e2e -w panel -- --list
npm run e2e -w panel
```

Expected: 7 desktop + 3 mobile tests discovered, then 10/10 PASS against the real seeded backend. If credentials/backend are unavailable, record `NOT RUN`; never substitute mocks and never report a pass.

- [ ] **Step 4: Record evidence without secrets**

The report and append-only ledger entry must include command, exit code, pass counts, bundle bytes, browser scope, and physical gates as `NOT RUN`. They must not include passwords, session tokens, QR values, bearer-bearing response bodies, or credential screenshots/traces.

- [ ] **Step 5: Commit only verification records if they changed**

```bash
git add .superpowers/sdd/progress.md .superpowers/sdd/2026-08-09-panel-p6.4-hardening-parity/station-assignee-correction-report.md
git diff --cached --check
git commit -m "docs(panel): record station assignee verification"
```

- [ ] **Step 6: Push and monitor PR #113**

```bash
git push origin feature/panel-p6.4-hardening-parity
gh pr checks 113 --watch --interval 20
```

Expected: required CI passes except any external GitGuardian incident that still needs dashboard disposition. Do not rewrite history for incident `35916563`; mark it false-positive/test credential only through the authorized GitGuardian dashboard. Fetch new review threads, address actionable findings with focused tests, and resolve a thread only after its fix is pushed.

- [ ] **Step 7: Final integrity check**

```bash
git status --short
git log --oneline --decorate -8
git diff --check origin/main...HEAD
```

Expected: clean worktree, intended commits only, and no whitespace errors.

---

### Task 14: Mark Retained Monitor Data Stale After Snapshot Refetch Failure

**Files:**
- Modify: `panel/src/features/monitor/MonitorPage.tsx:99-210`
- Test: `panel/src/features/monitor/MonitorPage.test.tsx:841-883`
- Modify: `panel/src/shared/i18n/en.json`
- Modify: `panel/src/shared/i18n/ru.json`
- Test unchanged: `panel/src/shared/i18n/keyParity.test.ts`

**Interfaces:**
- Consumes: an already-rendered snapshot, a still-live controlled SSE stream, and `snapshotQuery.isError` after failed background invalidation.
- Produces: retained content plus a localized stale-data status, warning counter, no LIVE pulse, and degraded body styling until a successful snapshot refresh; initial errors and stream reconnect/error states remain unchanged.

- [ ] **Step 1: Strengthen the retained-background-error test to prove live-to-stale truthfulness**

Use `controlledMonitorStreamHandler()`, push a real `hello` frame, and prove the page starts data-fresh. After the existing failed snapshot invalidation, require:

```tsx
expect(screen.getByTestId("monitor-data-stale-badge")).toHaveTextContent("Data refresh failed");
expect(screen.getByTestId("monitor-body")).toHaveClass(
  "opacity-60",
  "[&_.text-muted-foreground]:text-foreground",
);
expect(screen.getByTestId("monitor-updated-ago")).toHaveClass("text-warning");
expect(liveRing()).not.toBeInTheDocument();
```

Keep the existing retained event/totals/zone assertions and no initial-error-card assertion. Add exact RU/EN key-parity coverage through the existing parity test.

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- MonitorPage.test.tsx keyParity.test.ts
```

Expected: FAIL because a live stream currently makes failed-refetch data remain LIVE, full-opacity, and muted-fresh.

- [ ] **Step 3: Derive a truthful data-fresh state and render a distinct stale status**

```tsx
const streamLive = stream.status === "live";
const dataFresh = streamLive && !snapshotQuery.isError;
```

Use `dataFresh` for the LIVE pulse, counter tone/icon, and retained-body degradation. When retained snapshot data exists and `snapshotQuery.isError`, replace the primary LIVE presentation with a localized `StatusPill` identified by `monitor-data-stale-badge`, and announce the same copy through the existing polite stream announcer. Preserve stream-error priority and keep the reconnecting badge when the transport is reconnecting.

Add:

```json
"monitorDataStale": "Data refresh failed"
```

```json
"monitorDataStale": "Не удалось обновить данные"
```

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- MonitorPage.test.tsx keyParity.test.ts
npm run typecheck -w panel
npm run lint -w panel
```

Expected: focused tests and static gates exit 0; the same retained data is visibly degraded despite an open SSE after the snapshot refetch fails.

- [ ] **Step 5: Commit only the four changed files**

```bash
git add panel/src/features/monitor/MonitorPage.tsx panel/src/features/monitor/MonitorPage.test.tsx panel/src/shared/i18n/en.json panel/src/shared/i18n/ru.json
git diff --cached --check
git commit -m "fix(panel): mark failed monitor refresh stale"
```

---

### Task 15: Show Self-Service QR Mint and Regeneration Errors

**Files:**
- Modify: `panel/src/features/staff/SelfServicePage.tsx:15-98`
- Test: `panel/src/features/staff/SelfServicePage.test.tsx`
- Modify: `panel/src/shared/i18n/en.json`
- Modify: `panel/src/shared/i18n/ru.json`
- Test unchanged: `panel/src/shared/i18n/keyParity.test.ts`

**Interfaces:**
- Consumes: initial and regeneration failures from the existing QR-token mutation and the session-cached previous QR.
- Produces: localized visible `role=alert` feedback on both base and QR screens; failed regeneration retains the prior QR and re-enables retry; no token value enters alert copy or attributes.

- [ ] **Step 1: Write failing initial and regeneration error regressions**

Initial 500:

```tsx
expect(await screen.findByRole("alert")).toHaveTextContent("Не удалось создать QR для входа. Попробуйте ещё раз.");
expect(screen.getByRole("button", { name: "Показать мой QR для входа" })).toBeEnabled();
expect(screen.queryByTestId("qr-display-code")).not.toBeInTheDocument();
```

Regeneration 500 after a successful first mint:

```tsx
expect(await screen.findByRole("alert")).toHaveTextContent("Не удалось создать QR для входа. Попробуйте ещё раз.");
expect(screen.getByTestId("qr-display-code")).toBeInTheDocument();
expect(screen.getByRole("button", { name: "Показать мой QR для входа" })).toBeEnabled();
```

The second test must prove the base scanning-note/sign-out screen never replaces the cached QR after failure.

- [ ] **Step 2: Run focused RED**

```bash
npm test -w panel -- SelfServicePage.test.tsx keyParity.test.ts
```

Expected: FAIL because mutation errors currently only re-enable controls and render no feedback.

- [ ] **Step 3: Render one localized error on each active surface**

Add exact EN/RU copy:

```json
"selfServiceQrError": "Couldn't generate your login QR. Try again."
```

```json
"selfServiceQrError": "Не удалось создать QR для входа. Попробуйте ещё раз."
```

On the base page, render the alert adjacent to the mint action when `generateToken.isError`. On the QR screen, keep `QrDisplay` mounted from `cachedToken` and overlay or position a semantic-token alert without adding the bearer to text/attributes. A new `mutate()` attempt clears the mutation error; closing the QR session may reset it so a regeneration error does not leak onto a later base session.

- [ ] **Step 4: Run focused and static GREEN**

```bash
npm test -w panel -- SelfServicePage.test.tsx keyParity.test.ts
npm run typecheck -w panel
npm run lint -w panel
```

Expected: focused tests and static gates exit 0; initial failure is visible and retryable, failed regeneration retains the old QR with visible feedback.

- [ ] **Step 5: Commit only the four changed files**

```bash
git add panel/src/features/staff/SelfServicePage.tsx panel/src/features/staff/SelfServicePage.test.tsx panel/src/shared/i18n/en.json panel/src/shared/i18n/ru.json
git diff --cached --check
git commit -m "fix(panel): surface self-service QR errors"
```

---

### Task 16: Bind Self-Service QR Credentials to the Active Tenant and Live Role

**Files:**
- Create: `backend/migrations/000026_scope_user_qr_tokens.up.sql`
- Create: `backend/migrations/000026_scope_user_qr_tokens.down.sql`
- Modify: `backend/internal/models/models.go`
- Modify: `backend/internal/store/interface.go`
- Modify: `backend/internal/store/pg_store.go`
- Test: `backend/internal/store/pg_store_qr_token_test.go`
- Test if query shape changes: `backend/internal/store/pg_store_empty_list_test.go`
- Modify: `backend/internal/handler/users.go`
- Modify: `backend/internal/handler/qr_auth.go`
- Test: `backend/internal/handler/users_qrtoken_test.go`
- Test: `backend/internal/handler/openapi_contract_auth_test.go`
- Test: `backend/internal/handler/openapi_contract_session_test.go`
- Modify test fake: `backend/internal/handler/testsupport_test.go`
- Modify: `backend/openapi.yaml`
- Modify generated schema only if the OpenAPI generator produces a tracked semantic change: `panel/src/shared/api/schema.d.ts`

**Interfaces:**
- `UpdateUserQRToken` persists `tenantID` and `role` with the opaque token and creation time in storage invisible to legacy readers.
- `GetUserByQRToken` reads only the new scoped credential storage and returns internal-only scope fields on `models.User`; those fields never enter JSON.
- QR login issues a JWT and response user for the persisted active tenant and role only after proving the same live membership still exists.

- [ ] **Step 1: Add RED authorization and login-scope regressions**

Cover at least these cases without printing a QR value:

1. A JWT claiming `staff` cannot self-mint after the active membership changed to `manager`; expect 403 and no write.
2. A home-tenant admin whose active-tenant membership is `staff` mints a QR scoped to the active tenant and `staff` role.
3. QR login returns claims and response user scoped to that active tenant/role, not `users.tenant_id/users.role`.
4. QR login fails closed when the scoped membership was removed or its role changed after mint.
5. Legacy/unscoped QR rows fail closed.

Run focused RED before production edits:

```bash
cd backend && go test ./internal/handler -run 'TestGenerateQRToken|TestLoginWithQR|TestContract(LoginWithQR|GenerateQRToken)'
```

- [ ] **Step 2: Add rollback-safe storage invisible to legacy readers**

Migration `000026` creates a dedicated one-row-per-user scoped QR credential table containing the opaque token, active tenant FK, constrained role, and creation time. It clears the legacy `users.qr_token/qr_token_created_at` columns and adds a constraint keeping both legacy columns NULL. This makes rolling deployment fail closed: old writers cannot create global credentials after migration, and old readers cannot see credentials issued by the new writer. Tenant deletion cascades only the scoped credential row.

The down migration drops the scoped credential table and its legacy-write constraint only after ensuring the legacy columns remain empty, so rollback cannot reinterpret a tenant-scoped credential as a home/global credential.

Extend `models.User` with JSON-hidden scope fields, update the store interface/fakes, read only the dedicated table in `GetUserByQRToken`, and upsert token + tenant + role + time atomically in `UpdateUserQRToken`. Listing paths must derive `has_qr_token` and issuance time from the scoped table without selecting the raw token. Add pgxmock tests for update argument order, query/scan, listing metadata, and no-row/error behavior.

- [ ] **Step 3: Enforce live role on mint and login**

For staff self-service, require the caller/target identity match and the target's current active membership role equal exactly `staff`; a stale staff JWT with a current manager role is forbidden. Admin issuance retains its existing active-member behavior but persists that member's live tenant role.

At QR login, require a complete persisted scope, re-read `GetUserTenantRole(user.ID, scopedTenantID)`, and reject absent/mismatched membership before JWT issuance. Issue the JWT for the persisted scoped tenant/role and return a response copy whose `tenant_id` and `role` match those claims. Never log or expose the QR token.

- [ ] **Step 4: Run focused and backend GREEN**

```bash
cd backend && go test ./internal/handler -run 'TestGenerateQRToken|TestLoginWithQR|TestContract(LoginWithQR|GenerateQRToken)'
cd backend && go test ./...
cd backend && go build ./...
cd backend && golangci-lint run ./internal/...
```

- [ ] **Step 5: Commit only Task 16 files**

```bash
git add backend/migrations/000026_scope_user_qr_tokens.up.sql backend/migrations/000026_scope_user_qr_tokens.down.sql backend/internal/models/models.go backend/internal/store/interface.go backend/internal/store/pg_store.go backend/internal/store/pg_store_qr_token_test.go backend/internal/store/pg_store_empty_list_test.go backend/internal/handler/users.go backend/internal/handler/qr_auth.go backend/internal/handler/users_qrtoken_test.go backend/internal/handler/openapi_contract_auth_test.go backend/internal/handler/openapi_contract_session_test.go backend/internal/handler/testsupport_test.go backend/openapi.yaml panel/src/shared/api/schema.d.ts
git diff --cached --check
git commit -m "fix(backend): scope QR login to active tenant"
```

---

### Task 17: Return Event Staff with the Event Tenant's Live Role

**Files:**
- Modify: `backend/internal/store/pg_store.go`
- Test: `backend/internal/store/pg_store_event_staff_test.go`
- Modify: `backend/openapi.yaml`

**Interfaces:**
- `GetEventStaff(eventID)` keeps the existing `[]*models.User` shape but returns `tenant_id` and `role` from the event's current `user_tenants` membership, not the user's home/global columns.
- Event assignments whose tenant membership no longer exists are omitted, matching the station authorization boundary.

- [ ] **Step 1: Add RED cross-tenant role regressions**

Use the store test harness to prove a user with home tenant A/global `admin` and event tenant B membership `staff` is returned with tenant B and role `staff`. Add a role-change case showing the current membership role is returned. The prior query should fail these assertions.

- [ ] **Step 2: Join event ownership and live membership in one query**

Join `events` and `user_tenants` on the assigned user plus `events.tenant_id`, select `events.tenant_id` and `user_tenants.role`, and keep the existing assignment ordering. Do not fall back to `users.role`; missing memberships are ineligible and omitted.

Clarify the OpenAPI description that each returned User is projected into the event tenant with its current tenant membership role. The response schema shape remains unchanged.

- [ ] **Step 3: Run focused and backend GREEN**

```bash
cd backend && go test ./internal/store -run 'TestPGStoreGetEventStaff'
cd backend && go test ./...
cd backend && go build ./...
cd backend && golangci-lint run ./internal/...
```

- [ ] **Step 4: Commit only Task 17 files**

```bash
git add backend/internal/store/pg_store.go backend/internal/store/pg_store_event_staff_test.go backend/openapi.yaml
git diff --cached --check
git commit -m "fix(backend): use tenant roles for event staff"
```

---

### Task 18: Replace Remaining Wall-Clock Pending-State Windows with Explicit Gates

**Files:**
- Test: `panel/src/features/attendees/AddAttendeeDialog.test.tsx`
- Test: `panel/src/features/badge/BadgeEditorPage.test.tsx`
- Test: `panel/src/features/checkin/LaunchCeremony.test.tsx`
- Test: `panel/src/features/zones/ZoneFormDialog.test.tsx`
- Test: `panel/src/features/attendees/AttendeeDrawer.test.tsx`

**Interfaces:**
- Production components and network contracts remain unchanged.
- Each affected test controls request entry and settlement with an explicit deferred gate instead of assuming a 40-80ms pending window.
- Post-release assertions wait for positive observable completion before any negative session-guard assertion.

- [ ] **Step 1: Preserve full-suite RED evidence**

The default 135-file panel suite rotated through five failures while each exact focused rerun passed: AddAttendeeDialog pending dismissal (`createDelayMs=60`), BadgeEditor late save/cache (`PUT delay` plus `setTimeout(80)`), LaunchCeremony pending settings save (`delay(50)`), ZoneFormDialog pending dismissal (`createDelayMs=60`), and AttendeeDrawer pending print explanation (`printDelayMs=40`). The required consecutive count is zero.

- [ ] **Step 2: Replace only the affected timing windows**

Use test-local `entered` and `release` deferred gates. Wait for request entry before asserting pending UI/dismissal behavior. Always release in `finally`. After release, wait for a positive completion signal: successful close/refetch/cache version/action availability as appropriate. For BadgeEditor, remove the 80ms sleep and wait for the observable cache/refetch settlement proving the late response was processed before asserting the cross-event session guard.

- [ ] **Step 3: Run focused tests twice and static gates**

```bash
npm test -w panel -- AddAttendeeDialog.test.tsx BadgeEditorPage.test.tsx LaunchCeremony.test.tsx ZoneFormDialog.test.tsx AttendeeDrawer.test.tsx
npm test -w panel -- AddAttendeeDialog.test.tsx BadgeEditorPage.test.tsx LaunchCeremony.test.tsx ZoneFormDialog.test.tsx AttendeeDrawer.test.tsx
npm run typecheck -w panel
npm run lint -w panel
```

- [ ] **Step 4: Commit only the five test files**

```bash
git add panel/src/features/attendees/AddAttendeeDialog.test.tsx panel/src/features/badge/BadgeEditorPage.test.tsx panel/src/features/checkin/LaunchCeremony.test.tsx panel/src/features/zones/ZoneFormDialog.test.tsx panel/src/features/attendees/AttendeeDrawer.test.tsx
git diff --cached --check
git commit -m "test(panel): gate remaining pending-state races"
```

---

### Task 19: Repeat Full Verification, Publish, and Re-review the Whole Branch

**Files:**
- Modify append-only: `.superpowers/sdd/progress.md`
- Update report: `.superpowers/sdd/2026-08-09-panel-p6.4-station-assignee-correction/task-19-report.md`

- [ ] **Step 1: Run fresh backend, panel, and shared-UI gates**

Run all backend tests/build/lint; two consecutive full panel suites; UI tests/typecheck/lint; panel typecheck/lint/build/bundle. A failed panel run resets the consecutive count.

- [ ] **Step 2: Re-run E2E compile/discovery and real-backend mobile acceptance**

Require exact discovery of 7 desktop + 3 mobile. Because Task 16 changes QR persistence/login semantics, run the three serial mobile journeys against a fresh isolated PostgreSQL/backend stack. Keep credentials within the process environment, disable credential-bearing browser artifacts, and report only status/count/duration. Never reuse the pre-Task-16 mobile pass as final evidence.

- [ ] **Step 3: Commit evidence, push, and reconcile PR #113**

Push the branch only after the fresh gates pass, watch required checks, fetch review comments/threads, and address actionable findings. GitGuardian incident `35916563` remains a manual external disposition; do not rewrite history.

- [ ] **Step 4: Request a final independent whole-branch review**

The final reviewer must re-audit both prior backend findings, the Task 14-15 panel fixes, credential hygiene, CI wiring, and plan/ledger truthfulness. Completion requires no open Critical/Important findings and an explicit accounting of every `NOT RUN` physical/manual gate.

---

### Task 20: Close the Final CodeRabbit Accessibility, Error-State, and Test-Proof Gaps

**Files:**
- Modify: `panel/src/features/workspace/ReadinessStrip.tsx`
- Test: `panel/src/features/workspace/ReadinessStrip.test.tsx`
- Modify: `panel/src/features/staff/StaffCard.tsx`
- Test: `panel/src/features/staff/StaffCard.test.tsx`
- Test: `panel/src/app/App.test.tsx`
- Test: `panel/src/features/monitor/MonitorPage.test.tsx`
- Modify: `panel/src/features/workspace/AddStationAction.tsx`
- Test: `panel/src/features/workspace/AddStationAction.test.tsx`
- Modify: `panel/src/shared/i18n/en.json`
- Modify: `panel/src/shared/i18n/ru.json`
- Test: `panel/src/shared/i18n/keyParity.test.ts`
- Modify examples in this plan where the reviewed snippets are reproduced.

**Interfaces:**
- The phone readiness strip has its own localized region name instead of reusing the desktop workspace-rail label.
- Staff QR close-focus overrides Radix only when the exact opener still exists; otherwise Radix retains its default restoration path.
- Station provisioning failures are visible on both the base and cached-QR surfaces; a failed regeneration preserves the exact cached QR and never exposes its credential in copy, attributes, or assertion output.
- Sonner and Monitor regressions await/assert the exact state they claim to prove.

- [ ] **Step 1: Add focused RED regressions before production changes**

Add tests that fail on the current implementation for the dedicated readiness label, missing-opener close fallback, visible failed station regeneration with an unchanged cached QR, and the exact toast lifecycle. Strengthen the Monitor negative class proof into two independent assertions. Keep credential-derived QR comparisons boolean-only so a failure cannot serialize the SVG.

- [ ] **Step 2: Implement the smallest production fixes**

Add `readinessStripLabel` in EN and RU and use it only on the phone strip. In `StaffCard`, call `preventDefault()` only when `fullScreenTriggerRef.current` is available and will be focused. In `AddStationAction`, render the existing generic provisioning failure as a semantic `role="alert"` on whichever surface is active, retain the cached QR after a failed regeneration, and clear stale error state when a QR session closes or a new mint succeeds. Do not alter the provisioning request contract or expose the token.

- [ ] **Step 3: Correct the reviewed test examples and run focused GREEN**

Update the plan's readiness label, StaffCard focus fallback, Sonner wait, and Monitor independent-class examples to match the load-bearing tests. Run:

```bash
npm test -w panel -- ReadinessStrip.test.tsx StaffCard.test.tsx App.test.tsx MonitorPage.test.tsx AddStationAction.test.tsx keyParity.test.ts
npm run typecheck -w panel
npm run lint -w panel
```

- [ ] **Step 4: Run full panel gates, commit, publish, and reconcile review threads**

```bash
npm test -w panel
npm run build -w panel
git diff --check
```

Commit only the Task 20 files, push the branch, then reply inline and resolve each fixed review thread. For the already-fixed AttendeeDrawer thread, cite the positive list/readiness refetch completion proof from `cc385dc` before resolving it. Re-run required PR checks and request one scoped independent review of the complete Task 20 diff.
